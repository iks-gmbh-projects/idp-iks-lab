# Change Planning Prompt

Use this prompt before larger docs, governance, or catalog changes.

Plan a focused change for this IDP IKS lab. Preserve the MVP boundary, avoid runtime or Kubernetes expansion unless explicitly requested, identify affected docs and Backstage artifacts, define validation, and call out whether an ADR is required. Treat Port artifacts as legacy migration references unless the task explicitly asks for Port work.
