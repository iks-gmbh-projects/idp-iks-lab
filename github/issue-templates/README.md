# Historical Issue Template References

GitHub uses active issue forms from `.github/ISSUE_TEMPLATE/`.

The markdown files in this directory are retained only as historical references from the initial Port experiment. New workflow wording should refer to Backstage component/entity references rather than Port service identifiers.
